<Window x:Class="IT_company.MainWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IT_company"
        mc:Ignorable="d"
        Title="MainWindow" Height="239" Width="405">
    <Grid Margin="0,0,0,4">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>

        </Grid.RowDefinitions>
        <TextBox x:Name="LoginTBX" Grid.Row="0" Margin="10,36,10,0" materialDesign:HintAssist.Hint="Логин" VerticalAlignment="Center" />
        <PasswordBox x:Name="PasswordTBX" Grid.Row="1" Margin="10,36,10,0" materialDesign:HintAssist.Hint="Пароль" VerticalAlignment="Center" />
        <Button
            Style="{StaticResource MaterialDesignFlatSecondaryDarkBgButton}"
            ToolTip="MaterialDesignFlatSecondaryDarkBgButton"
            Content="Авторизоваться" Grid.Row="2" Click="Button_Click" Margin="0,23,0,0" />
        <TextBlock x:Name="Default" Grid.Row="2" VerticalAlignment="Top" Height="17" Margin="0,10,0,0" Text="" Foreground="Red"/>

    </Grid>
</Window>
